<div class="page">
    <%- include('../partials/pageTitle', { pageTitle: "TES Team" }); %>
    
    <% if (Object.keys(hod).length > 0) { %>
        <%- include('../partials/teamDivision', { divisionTitle: "Head of Department", division: hod }); %>
    <% } %>
    
    <% if (Object.keys(generalSecretary).length > 0) { %>
        <%- include('../partials/teamDivision', { divisionTitle: "General Secretary", division: generalSecretary }); %>
    <% } %>
    
    <% if (Object.keys(coordinators).length > 0) { %>
        <%- include('../partials/teamDivision', { divisionTitle: "Co-ordinators", division: coordinators }); %>
    <% } %>
    
    <% if (Object.keys(executives).length > 0) { %>
        <%- include('../partials/teamDivision', { divisionTitle: "Executives", division: executives }); %>
    <% } %>
</div>
